const initState = {
  isLogin: false,
  isLoading: false,
};

const updateSettingState = (state, action) => {
  switch (action.type) {
  }
};

export default updateSettingState;
export { initState };
